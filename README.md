# sforgcompare
Django application which compares metadata between two Salesforce environments and presents the differences

## Run Locally

`python manage.py runserver_plus --cert-file cert.pem --key-file key.pem`